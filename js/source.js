
        document.addEventListener("keydown", function (event) {
            if (event.ctrlKey) {
                event.preventDefault();
            }   
        });
        $(document).keydown(function(e){ 
            if(e.which === 123){ 
        
            return false; 
        
            } 
        
        }); 